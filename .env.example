# Copy to .env.local — never commit real values.

# Secret used to sign session cookies (min 32 chars). Required in production.
SESSION_SECRET=

# Development only: skip real auth and act as a demo merchant.
AUTH_DEV_BYPASS=true

# Development only: publish fake real-time events so the dashboard moves.
KANDROP_DEMO_EVENTS=true
